#include <stdio.h>

int main() {
    // int c, res;
    // c = getchar();
    // printf("%d\n", c);
    // res = putchar(c);
    // printf("\n%d", res);
    // return 0;

    // char str[100];
    // printf("Enter a value: ");
    // gets(str); // warning
    // // fgets(str, sizeof(str), stdin);
    // printf("\nYou entered: ");
    // puts(str);

    // int a;
    // long long aa;
    // float b;
    // char c[10];
    // scanf("%d - %lld%f%s", &a, &aa, &b, c);

    // printf("%d %lld %f %s\n", a, aa, b, c);

    // printf("%d %lld %.4f %s\n", a, aa, b, c);

    int c;
    while ((c = getchar()) != EOF) {
        putchar(c);
    }
}